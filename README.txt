Fallon Safety-Kleen Turnaround Tracker Pro - Shared Firebase Version

Replace the files in your GitHub repo with these files:
- index.html
- firebase-config.js
- manifest.webmanifest
- service-worker.js
- icon.svg

Then commit the changes and Vercel will redeploy automatically.

This version preloads SEQ, PESCO, and HT from the uploaded spreadsheets.
If cloud sync is already active, the app will add any missing default units into the shared Firebase data automatically.
